[package]
name = "acornos"
version = "0.1.0"
edition = "2021"
description = "AcornOS ISO builder - Alpine + OpenRC + musl + busybox"

[dependencies]
anyhow = "1.0"
clap = { version = "4", features = ["derive"] }
distro-spec = { path = "../distro-spec" }
distro-builder = { path = "../distro-builder" }

[dev-dependencies]
tempfile = "3"
